import 'package:flutter/material.dart';

class ErrorVersionPage extends StatelessWidget {
  const ErrorVersionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
